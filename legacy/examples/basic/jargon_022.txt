Code:
SET temperature (37)
IF temperature is greater than 25 THEN
    IF temperature is less than 35 THEN
        PRINT "Warm day"
    ELSE
        PRINT "Very hot"
    END
ELSE
    PRINT "Cool"
END

Expected Output:
Very hot

Explanation:
Nested condition checks whether the temperature falls within certain ranges, showcasing conditional branching.